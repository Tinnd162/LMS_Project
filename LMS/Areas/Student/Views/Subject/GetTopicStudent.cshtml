@model List<DAL.StudentView.CourseDetailsView>
@{
    ViewBag.Title = "GetTopicStudent";
    Layout = "~/Areas/Student/Views/Shared/_BlankStudentLayout.cshtml";
}

<h2>@Model[0].courseName</h2>
<div class="card">
    @foreach (var item in Model)
    {
        <div class="card-header">
            @item.topicTitle
        </div>
        <div class="card-body">
            <blockquote class="blockquote mb-0">
                <p>@item.documentTitle</p>
                <p><a href="~/Student/Submit/@item.eventID">@item.eventTitle</a></p>
            </blockquote>
        </div>

        /*foreach (var i in Model.Where(s=> s.topicID == item.topicID))*/
        @*{   <div class="card-body">
                    <blockquote class="blockquote mb-0">
                        <p>@i.documentTitle</p>
                        <p><a href="~/Student/Submit/@item.eventID">@i.eventTitle</a></p>
                    </blockquote>
                </div>
            }*@
    }
</div>

